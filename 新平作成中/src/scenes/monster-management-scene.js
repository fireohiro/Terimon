import Phaser from '../lib/phaser.js';

export default class MonsterManagementScene extends Phaser.Scene {
    constructor() {
        super({ key: 'MonsterManagementScene' });
    }

    create() {
        // モンスター管理画面の作成
    }
}