import Phaser from '../lib/phaser.js';

export default class MenuScene extends Phaser.Scene {
    constructor() {
        super({ key: 'MenuScene' });
    }

    create() {
        // メニューUIの作成
    }
}